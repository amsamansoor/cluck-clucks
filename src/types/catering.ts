export interface HeroContent {
  heroImage: string;   // image URL (imported file ka path)
  heading: string;     // "Birthday? Wedding? Office party?"
  tagline: string;     // long line describing service
  orderLink: string;   // Toast / Order URL
  requestLink: string; // Special order form ya mailto
}
